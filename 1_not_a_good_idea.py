# not a good idea to use digits as the first character for
# package names, filenames, identfiers, classes, methods or anything
# it is an anti-pattern because
# there are circumstances where this will cause trouble

# a good pattern is to use a letter or an underscore as the first character